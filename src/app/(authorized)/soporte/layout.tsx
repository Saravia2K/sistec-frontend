import { type Metadata } from "next";
import { type PropsWithChildren } from "react";

export const metadata: Metadata = {
  title: "SISTEC | Soporte",
};

export default function SoporteLayout({ children }: PropsWithChildren) {
  return children;
}
